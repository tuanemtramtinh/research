OPENAI_API_KEY=your_openai_api_key_here

# The following configs are for database and auth.
# They are currently NOT used while DB/auth are disabled.
# Uncomment and fill them only when you re-enable those features.
# MONGO_URL=mongodb+srv://<user>:<password>@<host>/<db>?retryWrites=true&w=majority
# DB_NAME=your_db_name
# ACCESS_TOKEN_SECRET=your_jwt_secret
# ACCESS_TOKEN_EXPIRES_IN=7d